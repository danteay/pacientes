# Code Signing Configuration (Optional)
# Only needed for signed macOS builds without security warnings

# Set to 'true' to disable code signing even when other variables are set
SKIP_CODE_SIGNING=true

# Apple Developer Account Credentials
# Required for code signing and notarization
# APPLE_ID=your-apple-id@example.com
# APPLE_ID_PASSWORD=xxxx-xxxx-xxxx-xxxx  # App-specific password from appleid.apple.com
# APPLE_TEAM_ID=XXXXXXXXXX  # 10-character team ID from developer.apple.com

# Code Signing Identity (Optional)
# If not set, will auto-detect "Developer ID Application" certificate
# SIGNING_IDENTITY=Developer ID Application: Your Name (TEAM_ID)

# Note: Never commit .env file with real credentials to version control!
